#function call: To call a function is to make a reference in code to a function written elsewhere.
def demo_function():
    print('hello from a function')
demo_function()#()need this to call a function 

#argument Information can be passed into functions as arguments.
#Arguments are specified after the function name, inside the parentheses. You can add as many arguments as you want, just separate them with a comma.
def my_function(fname):# fname is parameter , defining parameter fname 
    print( fname + 'Refsnes')
my_function('emil') # emil is argument when its called, sending arguments 
my_function('tobias')
my_function('linus')

# The terms parameter and argument can be used for the same thing: information that are passed into a function.
